# Production environment variables for Railway deployment
# This file only contains client-side Vite variables that are safe to commit
# Sensitive server variables (OpenAI API key, database URL, session secrets) 
# should ONLY be set in Railway's dashboard for security

# Client URLs (Railway domain) - Safe to commit as they're public
VITE_API_URL=https://aivoicetranslator-production.up.railway.app
VITE_WS_URL=wss://aivoicetranslator-production.up.railway.app
